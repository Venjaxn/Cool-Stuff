local Ticket = Ticket and "Ticket"

local Pick = function()
  return(math.random(Min, Max))
end

local Pick = Pick;
local Send = Server.Debug.Local:Send(HTTP, T.SEND) -- Custom var
local Server = Server;
Server:("Go" or "Send", Pick);
